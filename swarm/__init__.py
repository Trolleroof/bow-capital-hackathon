"""CombatOS swarm vertical — decentralized multi-agent RL (MAPPO/CTDE).

Phase 0 exposes the point-mass coverage env (`env`) and the WebSocket bus
(`bus`). Phases 1+ add MAPPO training, ONNX export, and edge inference.
"""
